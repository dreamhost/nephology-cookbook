# server_addr is the IP/hostname of the HTTP server running nephology
server_addr: <%= node['ipaddress'] %>
# mirror_addr is the IP/hostname of an HTTP server with an ubuntu mirror
mirror_addr: <%= node['ipaddress'] %>
db_name: nephology
db_username: root
db_password: <%= node['nephology']['password'] %>
db_type: mysql
db_host: localhost
db_socket: /var/run/mysql-nephology/mysqld.sock
